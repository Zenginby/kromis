# Kromis Studio — ortam değişkeni ŞABLONU (Faz 0 / Adım 8). Kopyala: cp .env.example .env
# `.env` git'te YOK (.gitignore); bu dosyaya asla gerçek değer yazılmaz —
# bekçisi tests/test_docker_kapisi.py (her anahtar satırı BOŞ olmak zorunda).
#
# İki bölüm, iki farklı okuyucu:
#  1. ALTYAPI — `compose.yaml` bunları `env_file` ile sürece verir; uygulama
#     `os.environ`dan okur (paths.py, Dockerfile).
#  2. SAĞLAYICI KİMLİKLERİ — DİKKAT: uygulama bu adları SÜREÇ ORTAMINDAN
#     OKUMAZ ve web'de bir DOSYADAN da okumaz (Faz 1 / 7). Değerler kullanıcı
#     başına, ŞİFRELİ olarak veri tabanında yaşar (`saglayici_kimlikleri`,
#     `ad` sütunu = buradaki ad; services/depo_kimlik_bilgisi.py) ve onları
#     Ayarlar paneli (`POST /api/settings`) yazar. Dondurulmuş masaüstü kabuğu
#     aynı adları `credentials.env` dosyasında tutar (`azure_client.read_env_values`)
#     ve 8. görevin içe aktarma aracı o dosyayı DB'ye taşır. Buradaki satırlar
#     yalnız AD ENVANTERİNİ belgeler: `.env`e değer yazmanın hiçbir etkisi
#     yoktur. Adlar `catalog.CREDENTIALS`tan türetilir (bekçi test aynı kümeyi
#     katalogdan yeniden kurar) — elle tutulan bir liste bir sağlayıcı
#     eklendiğinde sessizce eksik kalırdı (`fal_key`, tests/test_settings_route.py
#     dersi). Ortamdan okuma (12-factor) Faz 0 / 8'in açık bıraktığı takipti;
#     Faz 1 / 7 kararıyla KAPANDI: web'de anahtar ne dosyadan ne ortamdan,
#     DB'den — ortamdaki tek anahtar herkese ortak olurdu, oysa BYOK kullanıcı
#     başına; platform sahipli anahtar Faz 2'nin konusu.

# ── 1. Altyapı ────────────────────────────────────────────────────────────

# Yazılabilir veri kökü: output/, assets/, manifest'ler, hata.log ve (HOME
# buraya bağlı olduğu için) kimlik dosyası. Dockerfile zaten /data veriyor;
# konak dizini bağlanacaksa dizin uid 10001'e ait olmalı, yoksa /health 503.
KROMIS_DATA_DIR=/data

# uvicorn'un dinlediği port (konteyner İÇİ). Dışa açılan port compose.yaml'da.
PORT=8765

# PostgreSQL bağlantı dizesi (Faz 1 / 1. görev; services/db.py okur, alembic
# de aynı yerden). Biçim: postgresql+psycopg://kullanici:parola@konak:5432/db
# `docker compose up` bunu KENDİ verir (compose.yaml `environment:` bu satırı
# ezer: postgresql+psycopg://kromis:kromis@postgres:5432/kromis); konakta
# `uvicorn` koşturan geliştirici compose'un Postgres'ine
# `…@localhost:5432/kromis` ile bağlanır. Boş ya da yoksa uygulama AÇILIR ama
# `/health` `db_reachable:false` ile 503 verir — veri tabanı olmadan hesap yok.
# Testler bunu OKUMAZ (kendi geçici kümesini kurar; CI'da KROMIS_TEST_DATABASE_URL).
DATABASE_URL=

# ── 2. Sağlayıcı kimlikleri (AD ENVANTERİ — değer yazılmaz; yukarıdaki uyarıyı oku) ──
#
# Her sağlayıcının anahtarı ve (varsa) adresi. Adres boş bırakılırsa katalogdaki
# varsayılan kullanılır; Azure'da varsayılan yok, kaynağın kendi adresi gerekir.
# Web'de bu adlar `saglayici_kimlikleri.ad` sütununun değerleri; masaüstünde
# `credentials.env` satırları.

# azure_image: Azure OpenAI görsel (gpt-image): anahtar + kaynağın adresi
AZURE_IMAGE_API_KEY=
AZURE_IMAGE_BASE_URL=

# azure_chat: Azure OpenAI sohbet — boşsa görselin kimliğine düşer (credstore)
AZURE_CHAT_API_KEY=
AZURE_CHAT_BASE_URL=

# openai: OpenAI — adres yalnız uyumlu bir vekil (gateway) için
OPENAI_API_KEY=
OPENAI_BASE_URL=

# gemini: Google Gemini
GEMINI_API_KEY=
GEMINI_BASE_URL=

# anthropic: Anthropic — yalnız sohbet (görsel ucu yok)
ANTHROPIC_API_KEY=
ANTHROPIC_BASE_URL=

# azure_foundry: Azure AI Foundry (MAI / FLUX) — görselden AYRI host
AZURE_FOUNDRY_API_KEY=
AZURE_FOUNDRY_BASE_URL=

# fal: fal.ai — adres alanı formda yok, dosyaya elle yazılır
FAL_KEY=
FAL_BASE_URL=

# Sohbet dağıtım adı: katalog `wire_model` yazamadığı için kullanıcıdan alınır
# (Azure'da dağıtım adını kaynağın sahibi verir; catalog.ChatModel.wire_from_env).
AZURE_CHAT_DEPLOYMENT=

# ── 1b. Hesap ve e-posta (Faz 1 / 3. görev) ──────────────────────────────

# Uygulamanın DIŞ adresi, `https://studio.example.com` biçiminde (yol yok).
# İki iş görür (services/koken.py): başka siteden gelen POST'u ayıran köken
# kapısının izin listesi VE e-posta bağlantılarının tabanı. Boşsa uygulama
# isteğin kendi kökenini kullanır — yerelde ve compose'ta yeter; vekil
# arkasındaki dağıtımda ZORUNLU, yoksa doğrulama bağlantısı iç adresle çıkar.
KROMIS_KOKEN=

# E-posta arka ucu: `konsol` (geliştirme/test — iletiler `posta.log`a yazılır,
# ağa çıkılmaz) ya da `resend` (Resend HTTPS API, services/posta.py). Boşsa
# RESEND_API_KEY doluysa `resend`, değilse `konsol`. `resend` deyip anahtarı
# vermek 503 üretir, sessizce konsola DÜŞMEZ.
KROMIS_POSTA=

# Resend gönderen adresi, `Kromis Studio <no-reply@alan>` biçimi de geçer;
# alan adının Resend'de doğrulanmış (SPF/DKIM) olması gerekir. Konsol arka
# ucunda kullanılmaz.
KROMIS_POSTA_GONDEREN=

# Resend API anahtarı (https://resend.com → API Keys). Yalnız `resend` arka
# ucunda okunur; testler HİÇBİR ZAMAN Resend'e istek atmaz.
RESEND_API_KEY=

# Çerezlerin `Secure` bayrağı (services/cerez.py). Boşsa kural: DATABASE_URL
# verilmişse (web) AÇIK, verilmemişse (dondurulmuş kabuk, loopback http) KAPALI.
# `0` düz HTTP'de zorla kapatır — yalnız yerel compose.yaml için (Safari
# `localhost`ta bile düz HTTP'de Secure çerezi saklamıyor); TLS arkasında
# ASLA 0 verilmez.
KROMIS_GUVENLI_CEREZ=

# ── 1c. Sağlayıcı anahtarlarının şifreleme anahtarı (Faz 1 / 7) ─────────

# ZORUNLU (web'de, yani DATABASE_URL verilmişse): kullanıcıların Ayarlar
# panelinden girdiği sağlayıcı anahtarları bu anahtarla şifrelenip DB'ye
# yazılır (Fernet, services/sifre.py). Yoksa ya da bozuksa uygulama AÇILMAZ —
# sessiz bir varsayılan anahtar, "şifreli" sütunu depoyu okuyan herkese
# açardı. Biçim: 32 baytın urlsafe base64'ü. Üretmek için:
#   python -c "import secrets,base64;print(base64.urlsafe_b64encode(secrets.token_bytes(32)).decode())"
# DB YEDEĞİNDEN AYRI SAKLANIR: kaybolursa bütün kullanıcıların kayıtlı
# anahtarları okunamaz (belge §7 / §9). Döndürme: yeni anahtarı virgülle
# BAŞA ekle ("yeni,eski"), dağıt, satırlar yeniden şifrelendikten sonra
# eskisini düşür. Testler bunu OKUMAZ (tests/conftest.py sahte bir anahtar verir).
KROMIS_SECRET_KEY=
