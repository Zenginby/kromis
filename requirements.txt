fastapi==0.141.*
uvicorn[standard]==0.53.*
httpx==0.28.*
pytest==9.*
Pillow==12.*
python-multipart==0.0.*
pywebview==6.2.*

# Veri tabanı katmanı (Faz 1 / 1. görev, docs/faz1-veritabani-hesaplar.md):
# SQLAlchemy 2 + Alembic + psycopg 3. Üçü de ÇALIŞMA ZAMANI bağımlılığı —
# `requirements-dev.txt`e DEĞİL: sürücü olmadan uygulama açılır ama `/health`
# `db_reachable:false` ile 503 verir, yani konteyner imajına girmek zorunda.
#
# NEDEN psycopg 3, asyncpg DEĞİL: 46 rotanın 41'i senkron `def` ve Starlette
# threadpool'unda koşuyor; asyncpg yalnız async motor demekti. psycopg 3 tek
# sürücüyle hem `Engine` hem `AsyncEngine` açıyor (`postgresql+psycopg`).
# `[binary]` extra'sı kendi libpq'sunu taşıyan tekerleği kurar (ölçüldü:
# `psycopg_binary.libs/libpq-*.so.5` tekerleğin içinde) — `slim` imaja
# `libpq-dev` + derleyici katmanı gerekmiyor. cp313 ve cp314 tekerlekleri
# PyPI'da doğrulandı (CI 3.14, imaj 3.13).
SQLAlchemy==2.0.*
alembic==1.20.*
psycopg[binary]==3.3.*

# Parola özeti (Faz 1 / 3. görev): pwdlib + argon2id. Doğrudan `argon2-cffi`
# de olurdu; pwdlib'in kazancı algoritma göçünün hazır olması
# (`verify_and_update` — parametreler bir gün değişince eski özet girişte
# sessizce yenilenir). `[argon2]` extra'sı argon2-cffi 25.1 + bindings'i
# kurar; bindings tekerleği `cp36-abi3` (her CPython 3.x'e uyar), cffi 2.1
# cp314 tekerleği PyPI'da — 2026-09-17'de `pip download --python-version
# 3.14` ile doğrulandı (CI 3.14, imaj 3.13).
pwdlib[argon2]==0.3.*

# Sağlayıcı anahtarlarının şifrelenmesi (Faz 1 / 7. görev): Fernet + HKDF +
# MultiFernet döndürme (services/sifre.py). ÇALIŞMA ZAMANI bağımlılığı — anahtar
# olmadan `saglayici_kimlikleri` satırı ne yazılır ne okunur. Tekerlek
# `cp311-abi3` (her CPython ≥ 3.11'e uyar): 2026-09-17'de `pip download
# --python-version 3.14 --only-binary=:all:` ile doğrulandı (CI 3.14, imaj 3.13).
cryptography==50.0.*

# Hata izleme (Faz 2 / 9, K10): Sentry SDK — yalnız `SENTRY_DSN` verilmişse
# ithal edilir ve kurulur (services/hata_izleme.py, tembel); DSN yoksa süreç
# paketi hiç yüklemez. Yine de ÇALIŞMA ZAMANI dosyasında: imajda hazır dursun
# ki sahibin DSN'i panelden girmesi yeni imaj istemesin. Saf Python tekerleği
# (`py3-none-any`), cp313/cp314 sorunu yok; ~2 MB. Yama ve MINOR serbest
# (`2.*`): 2.x API'si (`max_request_body_size`, `isolation_scope`) tests/
# test_sentry.py ile mandallı, kırıcı bir 3.0 ancak pin ilerletilince gelir.
sentry-sdk==2.*

# Polar (Faz 4 / 3, K1 — Merchant of Record): resmî Python SDK. ÇALIŞMA ZAMANI
# bağımlılığı: `services/polar.istemci()` checkout/portal/ürün listesi için
# (4. görev) ve abonelik iptali (5. görev) SDK istemcisini kurar; webhook ucu ise
# SDK'nın da kullandığı `standardwebhooks` kütüphanesiyle (SDK'nın bağımlılığı,
# ayrıca pinlenmedi — SDK `>=1,<2` istiyor) imzayı doğrular, pydantic modeline
# GİRMEZ (gerekçe services/polar.py başında). Saf Python tekerleği
# (`py3-none-any`), pydantic ≥ 2.11 (FastAPI zaten getiriyor), httpx ≥ 0.28.1
# (üstteki pinle uyumlu), jsonpath-python. MINOR SABİT (`0.32.*`): SDK henüz
# 0.x, Speakeasy üretimi her minor'da model adlarını değiştirebiliyor; pin
# ilerletilirken tests/fixtures/polar/*.json SDK modelinden yeniden geçer
# (tests/test_odeme.py). 2026-09-21'de PyPI'dan indirilip kaynağı okundu:
# openapi 2026-04, `validate_event` = Standard Webhooks HMAC-SHA256.
polar-sdk==0.32.*

# Windows .NET köprüsü — pywebview'ın KENDİ dolaylı bağımlılıkları
# (`pythonnet; sys_platform=="win32"`), burada yalnız SÜRÜMLERİ sabitleniyor.
#
# NEDEN: pinsiz kaldığı sürece Windows paketinin içeriğini derleme GÜNÜ PyPI'de
# ne varsa o belirliyordu. Windows işi v0.5.2'de (2026-08-19) doğdu ve CI Python
# 3.14 kullanıyor; pythonnet 3.0.5 `Requires-Python: <3.14` olduğu için orada
# ZORUNLU olarak 3.1.0 (2026-05-23) + clr_loader 0.3.1 seçiliyordu. O ikili hiç
# açılarak doğrulanmamıştı ve kullanıcıda paket hiç açılmadı (2026-09-04,
# "Failed to resolve Python.Runtime.Loader.Initialize"). Sürümün YAZILI olması,
# bir dahaki sefere neyin değiştiğini tahmin değil kayıt yapar.
#
# İŞARETÇİ ŞART: bu dosya Linux (_test.yml) ve macOS'ta da kuruluyor; işaretçi
# olmadan pip oralara da .NET köprüsü kurar ve macOS paketine girer.
#
# Yama hanesi BİLEREK serbest (deponun geleneği, bkz. üstteki satırlar): bu
# kusuru düzeltecek bir 3.1.x kendiliğinden gelsin. Onu güvenli kılan şey
# `_paket-windows.yml`'deki açılış denetimi — pin ile kapı BİRLİKTE anlamlı,
# ayrı ayrı değil.
pythonnet==3.1.*; sys_platform == "win32"
clr-loader==0.3.*; sys_platform == "win32"
